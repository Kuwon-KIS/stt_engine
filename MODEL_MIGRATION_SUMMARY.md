# 두 폴더 역할 명확한 요약

## 📂 폴더 1: models--openai--whisper-large-v3-turbo (불필요)

| 항목 | 내용 |
|------|------|
| 크기 | ~100KB (거의 비어있음) |
| 파일 수 | 1개 (refs/main) |
| 용도 | Hugging Face 캐시 메타데이터 |
| 설명 | 이건 그냥 캐시일 뿐, 실제 모델이 아님 |
| 삭제 가능 | ✅ Yes (언제든 삭제해도 괜찮음) |
| 이관 필요 | ❌ No (필요없음) |

---

## 📂 폴더 2: openai_whisper-large-v3-turbo ⭐ (필수!)

| 항목 | 내용 |
|------|------|
| 크기 | **~1.5GB** ⚠️ (중요!) |
| 파일 수 | 41개 |
| 용도 | **실제 Whisper 모델 파일** |
| 설명 | 이게 STT를 하기 위해 반드시 필요한 폴더 |
| 핵심 파일 | model.safetensors (1.5GB) |
| 삭제 가능 | ❌ No (절대 삭제하면 안됨) |
| 이관 필수 | ✅ Yes (반드시 이관해야 함) |

---

## 💻 GPU 서버로 이관하는 3가지 방법

### 🥇 방법 1: 자동 이관 스크립트 (최강 추천!)

```bash
$ chmod +x migrate-to-gpu-server.sh
$ ./migrate-to-gpu-server.sh
```

**자동으로:**
1. 모델 파일 확인
2. 서버 정보 입력 받음
3. 모델 폴더 복사
4. 프로젝트 코드 배포
5. Docker Compose 시작
6. 상태 확인

**소요 시간:** ~10분  
**실패 확률:** 거의 없음 ✅

---

### 🥈 방법 2: SCP로 폴더 복사 (간단)

```bash
# 로컬에서 실행:
$ scp -r ./models/openai_whisper-large-v3-turbo user@server:/opt/stt_engine/models/

# 서버에서:
$ cd /opt/stt_engine
$ git clone https://github.com/Kuwon-KIS/stt_engine.git .
$ docker-compose up -d
```

**소요 시간:** ~5분  
**난이도:** 쉬움 ✅

---

### 🥉 방법 3: 서버에서 직접 다운로드 (오래 걸림)

```bash
# 서버에서:
$ cd /opt/stt_engine
$ git clone https://github.com/Kuwon-KIS/stt_engine.git .
$ docker build -t stt-engine:with-model -f Dockerfile.gpu .
$ docker-compose up -d
```

자동으로 서버에서 모델 다운로드 (Hugging Face에서 ~15-30분)

**소요 시간:** ~30분  
**장점:** 복사 필요 없음  
**단점:** 시간이 오래 걸림 ⏰

---

## ✅ 이관 전 확인사항

### 로컬에서
```bash
# model.safetensors 파일 크기가 1.5GB인지 확인
$ ls -lh ./models/openai_whisper-large-v3-turbo/model.safetensors
```

### 이관 전에
```bash
# SSH 연결 테스트
$ ssh user@server "echo OK"

# 서버 디스크 공간 확인 (최소 2GB)
$ ssh user@server "df -h /opt"
```

### 이관 후에
```bash
# 서버에서 파일 크기 확인 (1.5GB여야 함)
$ ssh user@server "du -sh /opt/stt_engine/models/"

# API 헬스 체크
$ curl http://server:8001/health
$ curl http://server:8000/health  # vLLM
```

---

## 🎯 권장 방법

**이미 로컬에서 다운로드 완료했으면:**
- **방법 1** (자동 스크립트) 또는 **방법 2** (SCP 복사)를 사용하세요!
